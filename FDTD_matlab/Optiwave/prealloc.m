% Matrices
Ex = zeros(yMesh,xMesh);
Ey = zeros(yMesh,xMesh);
Ez = zeros(yMesh,xMesh);
Ete = zeros(xyMesh);
Etm = zeros(xyMesh);
phix = zeros(xyMesh);
phiy = zeros(xyMesh);
phiz = zeros(xyMesh);
phite = zeros(xyMesh);
phitm = zeros(xyMesh);
phitetm = zeros(xyMesh);
Ax = zeros(xyMesh);
Ay = zeros(xyMesh);
Az = zeros(xyMesh);
Int = zeros(xyMesh);
phiPan = zeros(xyMesh);
orient = zeros(xyMesh);

% Reduced matrices for figures
ExFig = zeros(figMesh);
EyFig = zeros(figMesh);
EzFig = zeros(figMesh);
EteFig = zeros(figMesh);
EtmFig = zeros(figMesh);
phixFig = zeros(figMesh);
phiyFig = zeros(figMesh);
phizFig = zeros(figMesh);
phiteFig = zeros(figMesh);
phitmFig = zeros(figMesh);
phitetmFig = zeros(figMesh);
AxFig = zeros(figMesh);
AyFig = zeros(figMesh);
AzFig = zeros(figMesh);
IntFig = zeros(figMesh);
phiPanFig = zeros(figMesh);
orientFig = zeros(figMesh);

